{% extends 'base.html.twig' %}

{% block title %}Shelf{% endblock %}

{% block body %}
    <h1>Shelf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shelf.id }}</td>
            </tr>
            <tr>
                <th>ShelfIdentifier</th>
                <td>{{ shelf.shelfIdentifier }}</td>
            </tr>
            <tr>
                <th>Floor</th>
                <td>{{ shelf.floor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shelf_index') }}">back to list</a>

    <a href="{{ path('shelf_edit', {'id': shelf.id}) }}">edit</a>

    {{ include('shelf/_delete_form.html.twig') }}
{% endblock %}
